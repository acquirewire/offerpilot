# NEVER upload these — secrets, big folders, local data
.venv/
__pycache__/
*.pyc
.env
config.yaml
state.json
*.db
accounts*.db
.pw-profile/
out/
*.xlsx
jobtracker/examples/*.docx
jobtracker/examples/*.pdf

# Large media / build artifacts — keep these on the SSD, not in git
viral_clips/
_deploy.tgz
*.log
